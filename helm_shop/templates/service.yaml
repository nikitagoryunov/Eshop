apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_shop.fullname" . }}
  labels:
    {{- include "helm_shop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm_shop.selectorLabels" . | nindent 4 }}
